[build]
  publish = "."
  functions = "netlify/functions"

[build.environment]
  NODE_VERSION = "18"

# Busca as notícias todo dia às 00:00 (horário de Brasília = UTC-3, sem horário de verão)
[functions."fetch-news-background"]
  schedule = "0 3 * * *"

[[redirects]]
  from = "/api/news"
  to = "/.netlify/functions/get-news"
  status = 200
